'use strict';

/**
 * fan-gear-accessory router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::fan-gear-accessory.fan-gear-accessory');
