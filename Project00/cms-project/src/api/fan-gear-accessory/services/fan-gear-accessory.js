'use strict';

/**
 * fan-gear-accessory service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::fan-gear-accessory.fan-gear-accessory');
